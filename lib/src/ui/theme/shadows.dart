import 'package:flutter/material.dart';

@immutable
class AppShadows {
  const AppShadows._();

  static const BoxShadow shadowB30 = BoxShadow(
    color: Color(0x4CC7C7C7),
    blurRadius: 30,
    offset: Offset(0, 4),
    spreadRadius: 0,
  );

  static const BoxShadow shadowB10 = BoxShadow(
    color: Color(0x4CC7C7C7),
    blurRadius: 30,
    offset: Offset(0, 4),
    spreadRadius: 0,
  );
}
